# PaymentApiSample-CSharp
Payment API sample for creating a credit card token and making a payment with it.

## Setup

In Program.cs set the variables `ClientId` and `ClientSecret` to your Client ID and Client Secret.
